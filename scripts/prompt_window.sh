#!/bin/bash

# With specific Terminator profile named 'simulation_prompt'
    # Bigger font, no scroll bar, no cursor blinking, no title bar
# terminator -b -p simulation_prompt --geometry '710x250+1210+0'

# Without specific Terminator profile
terminator -b --geometry '710x250+1210+0'